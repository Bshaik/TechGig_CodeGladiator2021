//   https://www.geeksforgeeks.org/numbers-in-range-l-r-such-that-the-count-of-their-divisors-is-both-even-and-prime/
public class PrimeGame
{
	/*testCase = int(input())
		    
		    while testCase>0:
		        LR = list(map(int,input().strip().split()))
		        L = LR[0]
		        R = LR[1]
		        lst=[]
		        flag=1
		        for i in range(L,R+1):
		            flag=1
		            for j in range(2,i):
		                if i%j==0:
		                    flag=-1
		                    break
		            if flag == 1:        
		                lst.append(i)
	
		        n=len(lst)
		        if n>1:
		            print(max(lst)-min(lst))
		        elif flag==-1:
		            print(-1)
		        else:
		            print(0)
		        testCase -=1*/

}
